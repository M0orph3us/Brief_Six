{% extends 'base.html.twig' %}

{% block title %}Edit Response
{% endblock %}
{% block main %}
	<main>
		<h1>Edit Response</h1>

		{{ include('responses/_form.html.twig',  {button_label: 'Update'}) }}
		<a href="{{ path('threads', {id: thread.id}) }}">back to list</a>
		{{ include('responses/_delete.html.twig') }}
	</main>
{% endblock %}
